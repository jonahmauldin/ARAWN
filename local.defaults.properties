# ============================================================
#  COMMITTED fallback defaults — SAFE, EMPTY placeholders only.
#  The secrets plugin uses these when secrets.properties is absent
#  (e.g. a fresh clone or CI), so the project always builds.
#
#  NEVER put a real token here. Real values go in secrets.properties,
#  which is git-ignored.
#
#  These are forward-looking for Phase 4 (Module D — WiGLE export).
#  WiGLE accounts and API tokens are FREE. Phase 2 (Room) uses none of them.
# ============================================================

WIGLE_API_NAME=UNSET
WIGLE_API_TOKEN=UNSET
